# Core (required)
streamlit>=1.30
anthropic>=0.40
requests>=2.31

# Bundle ingestion (real CMS bundle). Pure .md/.txt needs neither.
pdfplumber>=0.11
python-docx>=1.1
# Scanned PDFs (optional OCR): pip install pytesseract  + install the Tesseract binary.

# Judge Z3 (numeric/temporal consistency)
z3-solver>=4.12

# Dev
pytest>=8.0
